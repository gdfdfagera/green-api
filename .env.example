# Port the HTTP server listens on
PORT=3000

# Base URL of the GREEN-API REST API.
# Default is the public endpoint; override only if you use a self-hosted instance.
GREEN_API_BASE_URL=https://api.green-api.com

# Request timeout (ms) for outgoing calls to GREEN-API.
GREEN_API_TIMEOUT_MS=15000
